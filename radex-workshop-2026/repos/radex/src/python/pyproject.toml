[build-system]
requires = [
    "setuptools",
    "Cython",
    "numpy",
]
build-backend = "setuptools.build_meta"

[project]
name = "radex"
version = "0.0.0"
dependencies = [
    "cloudpickle",
    "numpy",
]
